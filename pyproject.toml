[project]
name = "scale-docs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "zensical>=0.0.58",
    "mike",
]

# Zensical maintains a fork of mike for compatibility; this is transitional
# until Zensical ships native versioning support.
# https://zensical.org/docs/compatibility/mkdocs/mike/
[tool.uv.sources]
mike = { git = "https://github.com/squidfunk/mike.git" }
